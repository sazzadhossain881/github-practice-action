name: first-workflow
on: workflow_dispatch
jobs:
    first-job:
            runs-on: ubuntu-latest
            steps:
                - name: print grettings
                  run: echo "Hello World"
                - name : print Goodbye
                  run: echo "Goodbye"
